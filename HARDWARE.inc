
CONF_OSC:
    BANKSEL OSCCON
    MOVLW   0XF1
    MOVWF   OSCCON
    BANKSEL PORTA
    RETURN

CONF_PORTS:
    BANKSEL TRISA
    BANKSEL PORTA
    RETURN
    
CLR_REG:
    CLRF    TECLA	    ;este reguistre es para asignar un valor de acuerdo con la tecla que sea pulsada en el teclado
    CLRF    STATE	    ;ESTE REGISTRO INCREMENTA CADA VEZ QUE SE OPRIME UNA TECLA
    CLRF    AUX_0
    CLRF    SECUENCIA	    ;ESTE REGUISTRO ES PARA AVANSAR EN LA TABLA
    CLRF AUX		    ;ESTE REGUISTRO ES PARA SABER QUE TECLA FUE PULSADA

    CLRF    PUERTO
    
    CLRF    UNI
    CLRF    DEC
    CLRF    CEN

    
    CLRF    DATA_W
    
    CLRF    PAS0    ; GUARDAN LA CONTRASEÑA DIGITADA
    CLRF    PAS1
    CLRF    PAS2
    CLRF    PAS3
    
    CLRF    LCD_PASS	; CURSOR DE LA LCD CUANDO SE INGRESA CONTRASEÑAS
    CLRF    BITS    ; BITS,0 INDICA SI HAY ONO ERROR EN LA CONTRASEÑA INSERTADA
    CLRF    NUMBER  ; PARA CONVERTIR DE BINARIO A BCD
    CLRF    INTENTOS	; REGISTROS AUXILIARES DE NUMERO DE INTENTOS
    CLRF    INTEN  
    RETURN

    

